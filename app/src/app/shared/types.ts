export interface Blog {
    id?: number;
    title: string;
    text: string;
    poster: string;
    postedAt: string;
}

export interface Book {
    id?: number;
    name: string;
    author: string;
    poster: string;
    fileUrl: string;
    description: string;
}